{
  "name": "nettbureau_task",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["common","frontend", "backend"],
  "scripts": {
    "build:backend": "cd common && yarn build && cd .. && cd backend && yarn build",
    "start:backend": "(cd backend && yarn serve)",
    "start:frontend": "(cd frontend && yarn serve)"
  }
}
